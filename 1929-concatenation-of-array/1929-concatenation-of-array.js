/**
 * @param {number[]} nums
 * @return {number[]}
 */
var getConcatenation = function(nums) {
    let res=[]
    nums.forEach((v)=>{
        res.push(v)
    })
     nums.forEach((v)=>{
        res.push(v)
    })
    return res
};